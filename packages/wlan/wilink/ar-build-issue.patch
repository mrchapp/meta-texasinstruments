Index: git/platforms/os/common/build/linux/Makefile
===================================================================
--- git.orig/platforms/os/common/build/linux/Makefile	2010-05-14 12:18:00.000000000 +0200
+++ git/platforms/os/common/build/linux/Makefile	2010-05-14 13:37:17.001474000 +0200
@@ -129,7 +129,7 @@
 #
 $(OUTPUT_FILE):
 	$(MAKE) -C $(KERNEL_DIR) M=`pwd` ARCH=$(ARCH) CROSS_COMPILE=$(CROSS_COMPILE) modules
-	@$(CROSS_COMPILE)$(AR) rcs $@ $(OS_OBJS)
+	@$(AR) rcs $@ $(OS_OBJS)
 
 
 endif	# ifneq ($(KERNELRELEASE),)
Index: git/platforms/os/linux/build/Makefile
===================================================================
--- git.orig/platforms/os/linux/build/Makefile	2010-05-14 12:18:00.000000000 +0200
+++ git/platforms/os/linux/build/Makefile	2010-05-14 13:37:17.001506000 +0200
@@ -207,7 +207,7 @@
 #
 $(OUTPUT_FILE):
 	$(MAKE) -C $(KERNEL_DIR) M=`pwd` ARCH=$(ARCH) CROSS_COMPILE=$(CROSS_COMPILE) modules
-	$(CROSS_COMPILE)$(AR) rcs $(OUTPUT_LIB) $(OS_OBJS)
+	$(AR) rcs $(OUTPUT_LIB) $(OS_OBJS)
 	mv tiwlan_drv.ko $(OUTPUT_FILE)
 
 
Index: git/stad/build/linux/Makefile
===================================================================
--- git.orig/stad/build/linux/Makefile	2010-05-14 13:40:34.000046000 +0200
+++ git/stad/build/linux/Makefile	2010-05-14 13:40:43.000479000 +0200
@@ -125,7 +125,7 @@
 #
 $(OUTPUT_FILE):
 	$(MAKE) -C $(KERNEL_DIR) M=`pwd` ARCH=arm CROSS_COMPILE=$(CROSS_COMPILE) modules
-	@$(CROSS_COMPILE)$(AR) rcs $@ $(DK_OBJS)
+	@$(AR) rcs $@ $(DK_OBJS)
 
 
 endif	# ifneq ($(KERNELRELEASE),)
