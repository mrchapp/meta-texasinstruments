Index: git/wilink7/platforms/os/linux/build/Makefile
===================================================================
--- git.orig/wilink7/platforms/os/linux/build/Makefile	2009-12-22 16:32:37.000000000 +0100
+++ git/wilink7/platforms/os/linux/build/Makefile	2009-12-22 16:33:12.000000000 +0100
@@ -194,7 +194,7 @@
 #
 $(OUTPUT_FILE):
 	$(MAKE) -C $(KERNEL_DIR) M=`pwd` ARCH=arm CROSS_COMPILE=$(CROSS_COMPILE) modules
-	$(CROSS_COMPILE)$(AR) rcs $(OUTPUT_LIB) $(OS_OBJS)
+	$(AR) rcs $(OUTPUT_LIB) $(OS_OBJS)
 	mv tiwlan_drv.ko $(OUTPUT_FILE)
 
 endif	# ifneq ($(KERNELRELEASE),)
Index: git/wilink7/platforms/os/common/build/linux/Makefile
===================================================================
--- git.orig/wilink7/platforms/os/common/build/linux/Makefile	2009-12-22 16:33:27.000000000 +0100
+++ git/wilink7/platforms/os/common/build/linux/Makefile	2009-12-22 16:33:44.000000000 +0100
@@ -129,7 +129,7 @@
 #
 $(OUTPUT_FILE):
 	$(MAKE) -C $(KERNEL_DIR) M=`pwd` ARCH=arm CROSS_COMPILE=$(CROSS_COMPILE) modules
-	@$(CROSS_COMPILE)$(AR) rcs $@ $(OS_OBJS)
+	@$(AR) rcs $@ $(OS_OBJS)
 
 
 endif	# ifneq ($(KERNELRELEASE),)
Index: git/wilink7/CUDK/os/linux/src/makefile
===================================================================
--- git.orig/wilink7/CUDK/os/linux/src/makefile	2009-12-22 16:34:20.000000000 +0100
+++ git/wilink7/CUDK/os/linux/src/makefile	2009-12-22 16:34:42.000000000 +0100
@@ -121,7 +121,7 @@
 
 $(OUTPUT_DIR)/$(LIB_NAME).a: $(OBJS)
 	rm -f $@
-	$(CROSS_COMPILE)$(AR) -q $@ $^
+	$(AR) -q $@ $^
 
 $(OUTPUT_DIR)/$(LIB_NAME).so: $(OBJS)
 	rm -f $@
Index: git/wilink7/stad/build/linux/Makefile
===================================================================
--- git.orig/wilink7/stad/build/linux/Makefile	2009-12-22 16:34:53.000000000 +0100
+++ git/wilink7/stad/build/linux/Makefile	2009-12-22 16:35:08.000000000 +0100
@@ -125,7 +125,7 @@
 #
 $(OUTPUT_FILE):
 	$(MAKE) -C $(KERNEL_DIR) M=`pwd` ARCH=arm CROSS_COMPILE=$(CROSS_COMPILE) modules
-	@$(CROSS_COMPILE)$(AR) rcs $@ $(DK_OBJS)
+	@$(AR) rcs $@ $(DK_OBJS)
 
 
 endif	# ifneq ($(KERNELRELEASE),)
